// Texture implementation file.
// The minimal starter uses only header-defined constant textures.
// Keep this file so future texture types can be added without changing structure.

#include "shading/texture.h"
